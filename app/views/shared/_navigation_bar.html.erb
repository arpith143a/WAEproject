<nav class="navbar navbar-default navbar-fixed-top" role="navigation">
  <div class="container-fluid">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1">
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <!-- <a class="navbar-brand">BIDANDBUY.AIT</a> -->
      <a class="navbar-brand">BidBuy@AIT</a>

    </div>

    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <fo class="nav navbar-nav">
        <% if user_signed_in? %>
            <!-- <li class="dropdown">
              <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-expanded="true">Search by Category <span class="caret"></span></a>
              <ul class="dropdown-menu" role="menu">

              <% Category.all.group_by {|c| c.name}.each do |c, v| %>

                <li value='cat'><%= link_to c, show_path(:name => c) %></li>
                  <% end %>
              </select>

              </ul>
            </li> -->
            <li class="dropdown">
            <a id ='drop' class="dropdown-toggle" data-toggle="dropdown">

                     Search by Category
                <b class="caret"></b>
            </a>
              <ul class="dropdown-menu">
            <a href="#">
              <% Category.all.group_by {|c| c.name}.each do |c, v| %>
               <li value='cat'><%= link_to c, show_path(:name => c) %></li>
              <% end %>


            </a>
               </ul>
            </li>

              <!--<div id="search" style="display: block">-->
              <a class="navbar-brand">
                <%= form_tag({:controller => :site, :method => :get, :action => :search}) do %>

                    <%= text_field_tag :search, params[:search], :placeholder => "want to search product...",:size=>"65",:class => 'form-control' %>



                <% end %>
              </a>



        <% else %>
            <li class='dropdown'>
              <p class="navbar-brand">Category:</p>
            </li>
            <li>
              <a class="navbar-brand">to know.. Sign in</a>
            </li>

        <% end %>

        <!--
        <form class="navbar-form navbar-left" role="search">
          <div class="form-group">

          </div>
        -->
      </ul>


<ul class="nav navbar-nav"></ul>
      <ul class="nav navbar-nav navbar-right">

        <% if user_signed_in? %>





            <li  class="active">

              <%= link_to "Home", root_path, method: :get %>
            </li>
                <li  class="active">
                    <%= image_tag current_user.avatar.url(:thumb), class: "user" %>

                    </li>


            <li class="active">


              <%= link_to 'Profile', profile_path, method: :get %>
            </li>
            <li class="active">
              <%= link_to "Sign out", destroy_user_session_path, method: :delete %>
            </li>
            <li class="active">
              <% if current_user.admin? %>
                  <%= link_to "Admin Dashboard", rails_admin_path %>
              <% end %>
            </li>
        <% else %>
            <li class="active">
              <%= link_to "Sign in", new_user_session_path %>
            </li>
            <li>
              <%= link_to "Sign Up using Google+ AIT", main_app.root_url.to_s + 'auth/google_oauth2' %>
            </li>
        <% end %>
      </ul>
    </div>
  </div><!-- /.container-fluid -->
</nav>
